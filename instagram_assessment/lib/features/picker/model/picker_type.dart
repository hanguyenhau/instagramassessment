enum Picker {
  crop,
  filter,
  adjust,
  fit,
  tint,
  blur,
  sticker,
  text,
  draw,
  mask,
}

enum Fit {
  ratio,
  blur,
  color,
}

enum Adjust {
  brightness,
  contrast,
  saturation,
  hue,
  sepia,
}
